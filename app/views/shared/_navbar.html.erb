<div class="navbar navbar-inverse navbar-static-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "<span class='glyphicon glyphicon-record'></span> 
        CCTV Project 2013".html_safe, root_path, class: "navbar-brand" %>
    </div>
    <div class="navbar-collapse collapse" style="height: 1px;">
      <ul class="nav navbar-nav navbar-right">
        <% if current_user.roles == 'guest' %>
          <li>
            <%= link_to "<span class='glyphicon glyphicon-star-empty'></span> Favorite Channel".html_safe, my_favorite_path %>
          </li>
        <% end %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown"><span class="glyphicon glyphicon-cog"></span> Account</a>
          <ul class="dropdown-menu">
            <li><%= link_to 'Change Profile', edit_user_path(current_user.id) %></li>
            <li class="divider"></li>
            <li><%= link_to "SignOut", destroy_user_session_path, :method => :delete %></li>
          </ul>
        </li>
      </ul>
    </div><!--/.nav-collapse -->
  </div>
</div>
<!-- /.navbar -->